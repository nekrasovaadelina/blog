.comment-form (data-behavior="comment-form")
  = simple_form_for [ask_article, ask_comment], remote: true do |f|
    .form-inputs
      = f.input :text, required: true

    .form-actions
      = f.button :submit, "Add comment"
